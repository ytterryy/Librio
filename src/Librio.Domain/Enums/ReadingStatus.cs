namespace Librio.Domain.Enums
{
    public enum ReadingStatus
    {
        NotStarted,   // ще не почав
        InProgress,   // читається
        Completed,    // прочитано
        Abandoned     // покинув
    }
}
